<UserControl x:Class="RonVOReviver.UI.VOComboBoxFileList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:RonVOReviver.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid Margin="5,2,5,2">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Margin="0,0,0,2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Title}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock x:Name="TextBlockItemCount" Grid.Column="1" VerticalAlignment="Center"/>
            <ComboBox x:Name="ComboBoxCharacter" Grid.Column="2" SelectionChanged="ComboBoxCharacter_SelectionChanged" VerticalAlignment="Center" Margin="5,0,0,0"/>
        </Grid>
        <ListBox x:Name="ItemList" Grid.Row="1"/>
    </Grid>
</UserControl>

